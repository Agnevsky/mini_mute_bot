# Базовый режим: только бот для мьюта, никакой базы и сайта.
# Турнир поднимается вместе с docker-compose.tournament.yml — см. README.
services:
  bot:
    build: .
    env_file: .env
    restart: always
